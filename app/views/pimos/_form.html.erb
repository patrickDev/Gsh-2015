<%= form_for(@pimo) do |f| %>
  <% if @pimo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pimo.errors.count, "error") %> prohibited this pimo from being saved:</h2>

      <ul>
      <% @pimo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :area %><br>
    <%= f.text_field :area %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
