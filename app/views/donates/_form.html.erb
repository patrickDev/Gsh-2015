<%= form_for([@project, @donate]) do |f| %>
  <% if @donate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donate.errors.count, "error") %> prohibited this donate from being saved:</h2>

      <ul>
      <% @donate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
